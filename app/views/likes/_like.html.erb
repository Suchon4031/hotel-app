<% if user_signed_in? && hotel.liked_by?(current_user)%>
  <%=link_to "いいね済", hotel_likes_path(hotel.id),  method: :delete, class: "hotel_likes", remote: true %>
<% else %>
  <%=link_to "いいねする", hotel_likes_path(hotel.id), method: :post, class: "hotel_likes", remote: true %>
<% end %>